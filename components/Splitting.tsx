import React, { useEffect } from "react";
import Splitting from "splitting";

const SplittingJs = () => {
  useEffect(() => {
    Splitting();
  }, []);
  return null;
};

export default SplittingJs;
